# Registered snapshot payloads are validated by raw-byte SHA-256 (src/export/snapshot.py:load_snapshot).
# Pin them to LF so a Windows checkout (core.autocrlf=true) cannot rewrite the bytes.
snapshots/**/*.json text eol=lf
